# Postgres connection string from your provider (Neon, Vercel Postgres, Supabase, etc.)
# Example (Neon): postgresql://user:password@ep-xxxx.us-east-2.aws.neon.tech/neondb?sslmode=require
DATABASE_URL=

# Set to "false" only if your provider does NOT require SSL (rare)
# DATABASE_SSL=false
